package _1_read_write_files_using_streams;

public class FilePathExercise {

    /*
    create File and Path, ,convert to and vice
    try functions if exists etc.
    get subpaths, root, parent childs etc
    traverse current or provided directory name
    traverse using filestrims
    try static methods from nio2
    try resolve, try with follow links and no follow,
    create new, override, append files, and directories
    Path: relativize() normalize(), toRealPath() from symbolic link,
    gretName(), getNameCount(),
    delete(), deleteIfExists()
    isSame(), mismatch()


    */
}
